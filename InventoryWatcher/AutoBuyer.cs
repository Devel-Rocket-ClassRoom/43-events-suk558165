using System;
using System.Collections.Generic;
using System.Text;

class AutoBuyer
    {
    public void Auto(string name, int oldCount, int newCount)
    {
        Console.WriteLine($"[UI] {name}: {oldCount} → {newCount}");
    }
}

